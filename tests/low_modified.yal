(* Lexer de baja complejidad MODIFICADO: agrega division, parentesis y potencia *)

let delimitador = [' ''\t''\n']
let espacio     = delimitador+
let digito      = ['0'-'9']
let numero      = digito+
let letra       = ['a'-'z''A'-'Z']
let id          = letra (letra | digito)*

rule tokens =
    espacio   { }
  | id        { return ID }
  | numero    { return NUMBER }
  | '+'       { return PLUS }
  | '-'       { return MINUS }
  | '*'       { return TIMES }
  | '/'       { return DIV }
  | '^'       { return POWER }
  | '='       { return ASSIGN }
  | '('       { return LPAREN }
  | ')'       { return RPAREN }
